// Compatibility re-export. The canonical contracts ship with @askjo/camofox-browser-mcp
// so the standalone MCP package and the OpenClaw plugin use the same source.
export * from '../mcp/lib/tool-contracts.mjs';
